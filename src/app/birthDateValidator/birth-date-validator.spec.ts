import { BirthDateValidator } from './birth-date-validator';

describe('BirthDateValidator', () => {
  it('should create an instance', () => {
    expect(new BirthDateValidator()).toBeTruthy();
  });
});
